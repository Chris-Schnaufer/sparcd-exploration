import { useState } from 'react'; import { ConditionalReplaceConflictError, type SafeS3Client } from '@sparcd/s3-safe';
export type Registry={key:string;value:unknown[];etag:string;bucket:string}; const json=(x:unknown)=>JSON.stringify(x,null,2),id=()=>crypto.randomUUID();
const fields={Species:['name','scientificName','genus','species','keyBinding'],Locations:['nameProperty','idProperty','latProperty','lngProperty','elevationProperty','status','sensitive']} as const;
export function updateItem(items:Record<string,unknown>[],at:number,next:Record<string,unknown>){return items.map((v,i)=>i===at?next:v)}
export function RegistryEditor({title,registry,client,reload}:{title:'Species'|'Locations';registry:Registry;client:SafeS3Client;reload:()=>void}){const [items,setItems]=useState(registry.value as Record<string,unknown>[]),[selected,setSelected]=useState(0),[message,setMessage]=useState('');const item=items[selected]??{};const label=(x:Record<string,unknown>)=>String(x.name??x.nameProperty??x.scientificName??'New record');const change=(key:string,value:string)=>{const number=['latProperty','lngProperty','elevationProperty'].includes(key);setItems(updateItem(items,selected,{...item,[key]:number?Number(value):key==='sensitive'?value==='true':value}))};const save=async()=>{try{const at=new Date().toISOString(),eventId=id(),base=`Settings/audit/config/${at.slice(0,10)}/${eventId}`,event={schemaVersion:1,eventId,occurredAt:at,action:`${title.toLowerCase()}.updated`,target:{registryKey:registry.key},before:registry.value,after:items};await client.writeImmutable(registry.bucket,`${base}.prepared.json`,json(event),{contentType:'application/json'});const write=await client.replaceIfUnchanged(registry.bucket,registry.key,json(items),{etag:registry.etag,contentType:'application/json'});await client.writeImmutable(registry.bucket,`${base}.applied.json`,json({...event,appliedAt:new Date().toISOString(),afterETag:write.etag}),{contentType:'application/json'});setMessage('Saved and audited.');reload()}catch(e){setMessage(e instanceof ConditionalReplaceConflictError?'The registry changed elsewhere. Reload and review it before saving.':(e as Error).message)}};return <section><h2>{title}</h2><div className="records" role="list">{items.map((x,i)=><button role="listitem" aria-current={i===selected} onClick={()=>setSelected(i)} key={i}>{label(x)}</button>)}</div><button onClick={()=>{setItems([...items,{}]);setSelected(items.length)}}>Add {title.slice(0,-1)}</button><fieldset><legend>Edit {label(item)}</legend>{fields[title].map(key=><label key={key}>{key}<input aria-label={key} value={String(item[key]??'')} onChange={e=>change(key,e.target.value)}/></label>)}</fieldset><button onClick={()=>void save()}>Save {title}</button><p role="status">{message}</p></section>}
